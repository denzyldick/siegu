# Build/cache artifacts (keep the docker build context lean)
target
node_modules
dist
coverage
*.log
nvim.log
session-*.md
.DS_Store

# The Rust host stage copies everything from the repo root; exclude VCS/caches
# that would bloat the context.
.git
.gitignore
.githooks
