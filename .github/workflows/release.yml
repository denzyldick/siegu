name: Release

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  # ── Gate: only release if CI already passed for this commit ────────────
  # Avoids duplicating toolchain setup that CI already validates.
  # If CI hasn't run or failed, the release is blocked.
  ci-gate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Verify CI passed for HEAD
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          HEAD_SHA=$(git rev-parse HEAD)
          echo "Checking CI status for $HEAD_SHA"

          # Find the most recent completed CI run for this commit
          # Check each CI workflow (Ubuntu, Windows, macOS, Android, iOS)
          FAILED=0
          for wf in Ubuntu Windows macOS Android; do
            echo "--- Checking $wf CI ---"
            # Get the latest run for this workflow + commit
            RUN_ID=$(gh run list --workflow="${wf,,}.yml" --commit="$HEAD_SHA" \
              --status=completed --limit=1 --json databaseId --jq '.[0].databaseId' 2>/dev/null || true)
            if [ -z "$RUN_ID" ]; then
              echo "::warning::No completed $wf CI run found for $HEAD_SHA — skipping check"
              continue
            fi
            CONCLUSION=$(gh run view "$RUN_ID" --json conclusion --jq '.conclusion')
            echo "$wf CI run $RUN_ID concluded: $CONCLUSION"
            if [ "$CONCLUSION" != "success" ]; then
              echo "::error::$wf CI did not pass for $HEAD_SHA (conclusion: $CONCLUSION)"
              FAILED=1
            fi
          done

          if [ "$FAILED" -eq 1 ]; then
            echo "::error::CI has not passed for this commit. Fix CI before releasing."
            exit 1
          fi
          echo "All CI checks passed for $HEAD_SHA"

  # ── Desktop installers (macOS, Ubuntu, Windows) ────────────────────────
  desktop:
    needs: [ci-gate]
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        platform: [macos-latest, ubuntu-24.04, windows-latest]
    runs-on: ${{ matrix.platform }}
    env:
      ORT_STRATEGY: download
      TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ''

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          key: release-${{ matrix.platform }}
          workspaces: src-tauri

      - name: Install dependencies (Ubuntu only)
        if: matrix.platform == 'ubuntu-24.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libwebkit2gtk-4.1-dev libjavascriptcoregtk-4.1-dev libsoup-3.0-dev libjs-mathjax build-essential curl wget file libxdo-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev libavformat-dev libavcodec-dev libavutil-dev libswscale-dev libswresample-dev libavfilter-dev libavdevice-dev libpostproc-dev nasm

      - name: Install dependencies (macOS only)
        if: matrix.platform == 'macos-latest'
        run: |
          brew install nasm

      # Mirror of windows.yml CI setup: ffmpeg-sys-next builds FFmpeg from
      # source (features = ["build"]) and needs the full MSVC dev environment,
      # nasm, and a coherent MSYS2 sh+make+gawk toolchain. Omitting any of
      # these fails `tauri build` with "configure failed" before any installer
      # is produced.
      - name: Set up MSVC dev environment (Windows)
        if: matrix.platform == 'windows-latest'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Install nasm (Windows)
        if: matrix.platform == 'windows-latest'
        shell: pwsh
        run: choco install nasm -y --no-progress

      - name: Set up MSYS2 (make + gawk for the ffmpeg source build) (Windows)
        if: matrix.platform == 'windows-latest'
        id: msys2
        uses: msys2/setup-msys2@v2
        with:
          release: false # reuse the preinstalled C:\msys64
          update: false
          install: make gawk

      - name: Prepend MSYS2 tools to PATH (Windows)
        if: matrix.platform == 'windows-latest'
        shell: pwsh
        run: |
          $bin = Join-Path '${{ steps.msys2.outputs.msys2-location }}' 'usr\bin'
          # coreutils' link.exe would shadow MSVC's linker (rustc resolves
          # link.exe via PATH) and break every Rust link step.
          Remove-Item (Join-Path $bin 'link.exe') -Force -ErrorAction SilentlyContinue
          Write-Host "Prepending $bin to PATH"
          Add-Content $env:GITHUB_PATH $bin

      - run: npm ci

      - name: Prepare signing key
        shell: bash
        run: |
          KEY='${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}'
          # Only use the key when it looks like a real updater key: base64 that
          # decodes to UTF-8 and has the minisign/rsign "encrypted secret key"
          # box header (a pasted public key box is rejected). A malformed secret
          # otherwise kills every desktop build at the updater signing step (the
          # same `tauri build` that is green in CI with --no-bundle), so fall
          # back to an unsigned build instead of failing the release.
          if [ -n "$KEY" ] \
            && DECODED="$(printf '%s' "$KEY" | base64 -d 2>/dev/null)" \
            && printf '%s' "$DECODED" | iconv -f UTF-8 -t UTF-8 >/dev/null 2>&1 \
            && printf '%s' "$DECODED" | grep -q 'encrypted secret key'; then
            mkdir -p ~/.tauri
            printf '%s' "$KEY" > ~/.tauri/siegu.key
            echo "TAURI_SIGNING_PRIVATE_KEY=$HOME/.tauri/siegu.key" >> "$GITHUB_ENV"
            echo "SIGNING_READY=true" >> "$GITHUB_ENV"
          else
            echo "::warning::TAURI_SIGNING_PRIVATE_KEY missing or malformed (not a valid minisign key box); building unsigned installers."
            echo "SIGNING_READY=false" >> "$GITHUB_ENV"
          fi

      - name: Build Tauri App
        if: env.SIGNING_READY == 'true'
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'Siegu v__VERSION__'
          releaseBody: 'See the attached assets for the latest release versions.'
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.platform == 'ubuntu-24.04' && '--bundles appimage deb' || '' }}

      - name: Build Tauri App (unsigned)
        if: env.SIGNING_READY != 'true'
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'Siegu v__VERSION__'
          releaseBody: 'See the attached assets for the latest release versions.'
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.platform == 'ubuntu-24.04' && '--bundles appimage deb --no-sign' || '--no-sign' }}

  # ── Android APK ─────────────────────────────────────────────────────────
  android:
    needs: [ci-gate]
    permissions:
      contents: write
    runs-on: ubuntu-24.04
    env:
      ORT_STRATEGY: download
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - uses: android-actions/setup-android@v3
        with:
          packages: "platforms;android-34 build-tools;34.0.0 ndk;27.0.12077973"

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android
          components: rustfmt

      - uses: Swatinem/rust-cache@v2
        with:
          key: release-android
          workspaces: src-tauri

      - name: Install build dependencies
        run: sudo apt-get update && sudo apt-get install -y nasm

      - name: Install cargo-ndk
        run: cargo install cargo-ndk

      - name: Ensure rust targets are installed
        run: rustup target add aarch64-linux-android || true

      - name: Patch ffmpeg-sys-next for Android cross-compilation
        run: |
          cargo fetch --manifest-path src-tauri/Cargo.toml
          BUILD_RS=$(find ~/.cargo/registry/src -path "*/ffmpeg-sys-next-*/build.rs" -print -quit 2>/dev/null || true)
          if [ -z "$BUILD_RS" ]; then
            echo "ffmpeg-sys-next build.rs not found, skipping patch"
            exit 0
          fi
          echo "Patching $BUILD_RS"
          python3 scripts/patch-ffmpeg-android.py "$BUILD_RS"

      - run: npm ci

      - name: Build frontend
        run: yarn build

      - name: Build Rust lib for aarch64
        run: |
          cargo ndk -t aarch64-linux-android -P 24 \
            --manifest-path src-tauri/Cargo.toml \
            build --features "tauri/custom-protocol tauri/custom-protocol" --lib --release

      - name: Regenerate gitignored Gradle files (fresh init + tracked overlay)
        working-directory: src-tauri
        run: |
          # tauri.settings.gradle and app/tauri.build.gradle.kts carry absolute
          # paths into the local cargo registry, so they are gitignored and must
          # be regenerated per machine. Running `tauri android init` against the
          # existing tracked project does NOT rewrite them (it bails out after
          # path fixups, which is why the release failed with "Could not read
          # script .../tauri.settings.gradle"). Instead: snapshot every TRACKED
          # file, scaffold a completely fresh project (which emits all
          # gitignored files with correct machine-specific paths), then overlay
          # the tracked customizations back on top.
          rm -rf /tmp/fresh-android && mkdir -p /tmp/fresh-android
          git ls-files -z gen/android | tar --null -T - -cf - | tar -xf - -C /tmp/fresh-android
          rm -rf gen/android
          npx tauri android init --ci --skip-targets-install
          cp -a /tmp/fresh-android/src-tauri/gen/android/. gen/android/
          # init also re-emits its stock buildSrc under a fresh package dir; the
          # tracked copy is customized (cargo-ndk + -PskipRustBuild) and shares
          # the default package with the regenerated one, which would make Gradle
          # fail to compile buildSrc on duplicate RustPlugin classes.
          rm -rf gen/android/buildSrc/src/main/java/io/denzyl/siegu

      - name: Copy .so to jniLibs
        run: |
          mkdir -p src-tauri/gen/android/app/src/main/jniLibs/arm64-v8a
          cp target/aarch64-linux-android/release/libsiegu_lib.so \
             src-tauri/gen/android/app/src/main/jniLibs/arm64-v8a/

      - name: Build APK
        working-directory: src-tauri/gen/android
        # The .so was already built with cargo-ndk and copied into jniLibs
        # above; skip the in-Gradle Rust rebuild (which would also need the
        # x86_64-linux-android target this job does not install).
        run: ./gradlew assembleUniversalRelease -PskipRustBuild=true

      - name: Upload APK to Release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: src-tauri/gen/android/app/build/outputs/apk/universal/release/*.apk

  # ── Arch Linux AppImage ─────────────────────────────────────────────────
  arch:
    needs: [ci-gate]
    permissions:
      contents: write
    runs-on: ubuntu-24.04
    env:
      ORT_STRATEGY: download
    container:
      image: archlinux:base-devel
    # FFmpeg is bundled (features = ["build"]), so the rolling-Arch FFmpeg no
    # longer affects this build. The job is still allowed to fail without
    # blocking the release so any Arch-specific issue stays visible.
    continue-on-error: true
    steps:
      - name: Install dependencies
        run: |
          pacman -Syu --noconfirm
          # clang provides libclang, which ffmpeg-sys-next's bindgen step
          # requires while generating FFI bindings for the bundled FFmpeg build.
          pacman -S --noconfirm git nodejs npm rustup webkit2gtk-4.1 libayatana-appindicator librsvg xdotool openssl sudo yarn xdg-utils fuse2 ffmpeg nasm pkgconf clang

          useradd -m -G wheel builder
          echo "builder ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers

      - uses: actions/checkout@v4

      - name: Build Arch AppImage
        run: |
          chown -R builder:builder .
          su builder -c "
            rustup default stable
            npm ci
            APPIMAGE_EXTRACT_AND_RUN=1 npm run tauri build -- --bundles appimage
          "

      - name: Upload Arch AppImage
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: src-tauri/target/release/bundle/appimage/*.AppImage

  # ── iOS ─────────────────────────────────────────────────────────────────
  ios:
    needs: [ci-gate]
    permissions:
      contents: write
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-ios, x86_64-apple-ios, aarch64-apple-ios-sim

      - uses: Swatinem/rust-cache@v2
        with:
          key: release-ios
          workspaces: src-tauri

      - run: npm ci

      - run: npm run tauri ios build || echo "iOS build skipped/failed. Please configure code signing."
